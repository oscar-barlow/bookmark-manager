<h1> Welcome <%=  %></h1>

<ol id='links'>
 <% @links.each do |link| %>
   <li>
     Title: <%= link.title %>
     URL: <%= link.url %>
    Tags: <%= link.tags.map(&:name) %>
   </li>
 <% end %>
</ol>

<form action='/links/new'>
 <input type='submit' value="Create new link" />
</form>
